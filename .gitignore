# Terraform sensitive vars
*.auto.tfvars
terraform.tfvars

# Terraform state (never commit)
*.tfstate
*.tfstate.*

# Terraform internals
.terraform/
.terraform.lock.hcl

# Terraform plans (temporary binary artifacts)
*.tfplan
tfplan*

# Smoke outputs and evidences generated locally
outputs/
inputs/